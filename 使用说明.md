# 液态玻璃个人主页使用说明

这是一个纯静态网站，不需要服务器、数据库或安装依赖。修改完成后，可以直接发布到 GitHub Pages、Cloudflare Pages、Netlify 等静态托管平台。

## 快速预览

直接双击 `index.html` 即可在浏览器中预览。

## 修改个人资料与链接

用记事本或任意代码编辑器打开 `index.html`，搜索并替换下面这些现有内容：

- `KrKree`：昵称
- `@KrKree`：账号名
- `葛城リーリヤP担当、旮旯给木萌新`：个人简介
- 每个链接卡片中的网址、标题和副标题

链接卡片都位于 `<main class="links">` 区域。可以复制一整段 `<a class="link-card">...</a>` 来增加普通链接。

QQ 卡片使用弹窗显示二维码；弹窗代码位于文件末尾的 `<div id="qq-qr">...</div>`。

## 替换图片

保持文件名不变，直接覆盖即可：

- `avatar.png`：主页头像，建议使用正方形图片
- `background.png`：页面背景，建议使用横向高清图片
- `idolmaster.png`：学园偶像大师卡片头像
- `qq-qr.jpg`：点击 QQ 卡片后显示的二维码

`icons` 文件夹存放各平台图标。若不需要某个平台，可以在 `index.html` 中删除对应的链接卡片。

## 调整外观

所有颜色、透明度、圆角和手机端布局都在 `styles.css` 中。常用位置：

- `:root`：主题颜色
- `.glass-card`：中央液态玻璃面板
- `.link-card`：链接卡片
- `@media`：手机端适配

## 发布到 GitHub Pages

1. 在 GitHub 新建一个公开仓库。
2. 把本文件夹里的所有文件上传到仓库根目录。
3. 打开仓库的 `Settings → Pages`。
4. 在 `Build and deployment` 中选择 `Deploy from a branch`。
5. 选择 `main` 分支和 `/ (root)`，保存并等待发布。

发布后，访问地址通常为：

`https://你的GitHub用户名.github.io/仓库名/`

## 隐私提醒

分享或发布前，请务必替换原有账号链接、邮箱、QQ 号及 `qq-qr.jpg`，避免误用他人的个人信息。
